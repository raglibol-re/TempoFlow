/**
 * Advertiser agent (Phase 4): autonomous payer that streams payment to viewer
 * attention endpoints while heartbeats are valid, respecting a campaign budget.
 * Headless, CLI-driven. See docs/03-agent.md.
 */
console.log("[advertiser-agent] placeholder — implemented in Phase 4");
